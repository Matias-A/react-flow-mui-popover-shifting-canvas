# React Flow shifting canvas bug reproduction

To run:
```
npm install
npm start
```

To reproduce bug:
- move canvas so that one of the nodes is partially below its bottom
- right click on the visible part of said node